name: cPanel publish
on: 
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.namanprat.com
        username: ${{ secrets.ftp_username }}
        password: ${{ secrets.ftp_password }}
        # server.dir: namanprat.com